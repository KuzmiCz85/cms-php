<?php
  $items = isset($data->items) ? $data->items : null;
?>

<nav class="main-nav">
  <ul class="main-nav__list">
    <?php foreach ($items as $item) : ?>
      <li><a href="<?= $item->url_slug ?>"><?= $item->name ?></a></li>
    <?php endforeach ?>
  </ul>
</nav>
