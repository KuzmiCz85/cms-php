<?php
  $name = isset($field['name']) ? $field['name'] : null;
  $type = in_array($field['type'], ["text_line"]) ? $field['type'] : null;
?>

<div class="field" data-field-name="<?= $name ?>">
  <?php if ($type === "text_line") : ?>
    <input type="text" value="test-value" />
      type is text line
  <?php endif ?>
</div>
