<?php
  $action = isset($data->action) ? $data->action : null;
  $title = isset($data->title) ? $data->title : "button";

  $attrs = implode(' ', [
    'class="btn"',
    $action ? 'data-action="' . $action . '"' : null,
  ]);
?>

<button <?= $attrs ?>>
  <span class="btn__title"><?= $title ?></span>
</button>
